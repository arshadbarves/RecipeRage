namespace RecipeRage.UI.Core
{
    /// <summary>
    /// Defines the unique identifiers for each screen in the UI system.
    /// </summary>
    public enum ScreenId
    {
        None = 0, // Default/invalid state
        SplashScreen = 1,
        LoadingScreen = 2,
        MainScreen = 3,
        // Add more screen IDs here as needed
        // Example: SettingsScreen, ShopScreen, etc.
    }
}
